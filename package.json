{
  "name": "loyalty-protocol",
  "version": "3.0.8",
  "description": "",
  "main": "dist/index",
  "exports": {
    ".": "./dist/index.js",
    "./config": "./dist/config",
    "./theme": "./dist/theme.css",
    "./theme.css": "./dist/theme.css"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsup config/config.js --format esm --out-dir dist --minify && csso theme/theme.css --output dist/theme.css && tsc --outDir dist --declaration"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "private": false,
  "files": [
    "dist"
  ],
  "dependencies": {
    "crypto-js": "4.2.0"
  },
  "devDependencies": {
    "@types/crypto-js": "^4.2.2",
    "@types/node": "^25.0.3",
    "csso-cli": "^4.0.2",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
